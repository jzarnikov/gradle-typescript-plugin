apply plugin: 'groovy'

dependencies {
    compile gradleApi()
    compile localGroovy()
}

apply plugin: 'maven'

repositories {
    mavenCentral()
}

group = "org.typescriptlang.gradle"
version = "0.1-SNAPSHOT"

task wrapper(type: Wrapper) {
    gradleVersion = '1.10'
}